# Security Remediation — 2025-10-25

Summary
- Goal: remove critical/high vulnerabilities discovered by audit while minimizing risk.
- Actions taken (low-risk, targeted):
  - Bumped direct deps and added overrides where appropriate:
    - Added/updated `axios` => ^1.12.2 (patched CVE-2025-58754 path)
    - Bumped `postcss` => 8.4.31 (patched CVE-2023-44270 path)
    - Added `micromatch` dev pin => 4.0.8 (patched CVE-2024-4067)
    - Pinned `brace-expansion` => 1.1.12 (patched CVE-2025-5889) via `overrides` and devDependency
  - Upgraded `lint-staged` from 14.x → 16.2.6 to remove a transitive micromatch vulnerability.
  - Verified lockfile and node_modules by running `pnpm install`.

Verification
- Audit snapshot: `test-results/pnpm-audit-2025-10-25.json` (included in the branch).
- Latest `pnpm audit` returned 0 vulnerabilities (critical/high/moderate/low).
- Ran `pnpm build` — production build succeeded.
- Ran `pnpm run lint` — lint passed; one prior warning in tests was fixed.

Rationale
- Prioritized dev/tooling transitive fixes first (these posed the found vulnerabilities). Changes are minimal and low-risk.
- For nested dev-tool chains (e.g., `eslint` -> `minimatch` -> `brace-expansion`), pinned a patched 1.x root copy plus an `overrides` entry — this avoids large toolchain upgrades during the migration.

Follow-ups (recommended)
1. Merge this branch quickly (low-risk). The repo is now free of high/critical vulnerabilities.
2. Schedule an automated daily/weekly dependency audit run in CI that uploads the audit JSON and fails for high/critical advisories.
3. Periodically (quarterly) plan to upgrade major dev tools (eslint, sucrase, etc.) in a dedicated PR with CI verification.
4. Create an internal issue tracking acceptance testing for dev-tool upgrades (a checklist for human review).

Notes
- Peer-dependency warnings for some packages (next-intl, next-themes, react-day-picker, vaul) remain; these relate to React/Next major versions and are informational.

Signed-off-by: automated remediation script

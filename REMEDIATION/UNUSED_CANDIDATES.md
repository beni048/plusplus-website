# UNUSED CANDIDATES (automated scan)

This file was generated from the automated static analysis run on the branch `mcp-audit/20251025-safe-audit`.
It aggregates results from:

- `test-results/ts-unused-exports.txt` (ts-unused-exports)
- `test-results/depcheck.json` (depcheck)
- `test-results/eslint.json` (ESLint scan)

Purpose
- Provide a prioritized, reviewable list of potential unused exports/files.
- Do NOT mass-delete files automatically. Each candidate must be manually triaged and removed in a small, reversible PR with CI and smoke tests.

Quick summary
- Modules flagged by ts-unused-exports: 66
- depcheck had limited success in a Next app and should be treated as advisory. See `test-results/depcheck.json`.
- ESLint scan produced no errors for the scanned paths.

How to use this document
1. Review Group S (generated artifacts) and remove if committed to the repo. These are safe to delete.
2. For each candidate in Group R (requires review):
   - Open a small branch/PR removing the file.
   - Run CI and a minimal Playwright smoke test locally (or rely on CI).
   - If PR passes, merge; otherwise revert and document findings.

Group S — Generated / safe-to-delete (if present in the repo)
- `.next/types/*` (these are build artifacts generated by Next). If you see them committed, remove and add `.next/` to `.gitignore`.

Group R — Candidates requiring manual review
(list exported symbol paths from `ts-unused-exports` grouped for reviewer convenience)

